import { readFileSync } from 'fs';

const input = readFileSync('day??.txt', 'utf-8').replace(/\r/g, '').trim()

const firstPart = () => {
  // do something
}

const secondPart = () => {
  // do something
}